const express = require('express');
const router = express.Router();
// const coursesController = require('../controllers/coursesController');

// TODO: Add course CRUD, quiz, certificate routes

module.exports = router; 